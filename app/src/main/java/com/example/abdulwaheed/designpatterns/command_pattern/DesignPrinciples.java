package com.example.abdulwaheed.designpatterns.command_pattern;

/*
 * this class is intended to write all design principles used in this specific design patters.
 * It will be holding all rules of this design pattern in a same and single place.
 * */
public class DesignPrinciples {
    /*
     * Design Pattern Rules
     * ====================
     *
     * Definition
     * ==========
     *
     * The Command Pattern encapsulates a request as an object, thereby letting you parameterized
     * other objects with different requests, queue or log requests and supports undoable operations
     * */
}
