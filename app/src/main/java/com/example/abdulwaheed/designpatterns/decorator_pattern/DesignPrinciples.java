package com.example.abdulwaheed.designpatterns.decorator_pattern;

/*
 * this class is intended to write all design principles used in this specific design patters.
 * It will be holding all rules of this design pattern in a same and single place.
 * */
public class DesignPrinciples {
    /*
     * Design Pattern Rules
     * ====================
     * 1) Classes should be open for extension but closed for modification
     *
     * Definition
     * ==========
     * The Decorator Pattern attaches additional responsibilities to an object dynamically. Decorators
     * provide a flexible alternative to subclassing for extending fucntionality
     * */
}
